from setuptools import setup, find_packages

setup(
    name='automed',
    version='0.1',
    packages=find_packages(),
    author='MERIEUX Rudy',
    description='Automed was created to make machine learning as simple as possible'
)